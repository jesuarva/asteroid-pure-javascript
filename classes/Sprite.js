class Sprite {
  constructor(x, y) {
    let y = y;
    let speedX = 0;
    let speedY = 0;
    let x = x;
    let angle = 0;
    const shape = [];
    let darkSide = true;
  }
  draw() {
    //shape.forEach((point) => {})
  }
  move() {
    x += speedX;
    x += speedX;
  }
}
